import React, { Component } from 'react'
import {
  StyleSheet,
  PanResponder,
  TouchableOpacity,
  View,
} from 'react-native'

export default class NavSwiper extends Component {
  // TODO: Create component to respond to swiping gestures at edge of screen
}

const style = StyleSheet.create({
  bubble: {
    backgroundColor: 'pink',
    width: 80,
    height: 80,
  }
})
